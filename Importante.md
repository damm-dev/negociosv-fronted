si quieren probar la rama main en local, cambiar en el archivo .env 
pueden comentar el siguiente código VITE_API_URL=https://negociosv.com/api 
y agregan: VITE_API_URL=http://localhost:8000/api 

al subir cambios recodar borrar el localhost! si no,
tendremos problemas en el despliegue 

